
{% block body %}
<div class="cd-content-wrapper">
<div class="text-component text-center">
  {% for entry in object_list.items %}
    <p><a href="{{ url_for('entries.index', slug=entry.slug) }}">{{ entry.title }}</a></p>
    {% endfor %}
</div></div>
{% endblock %}
